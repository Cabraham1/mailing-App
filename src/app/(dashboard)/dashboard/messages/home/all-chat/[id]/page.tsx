import React from "react";
import Single from "../../../../../../../../components/single-chat";
const SingleChat = ({ params }: { params: any }) => {
  return <Single params={params} />;
};

export default SingleChat;
